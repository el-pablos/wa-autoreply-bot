services:
  mysql:
    ports: []   # Jangan expose port MySQL ke publik di prod

  bot:
    ports: []   # Tidak perlu expose bot port ke publik

  dashboard:
    ports:
      - "8002:80"
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
